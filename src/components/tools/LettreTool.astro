---
import Layout from "../../layouts/Layout.astro";
import { translations, slugs } from "../../i18n/translations.js";

interface Props {
  lang: "fr" | "en";
}

const { lang } = Astro.props;
const t = translations[lang].pages.lettre;
const common = translations[lang].common;
const prefix = lang === "en" ? "/en" : "";
const slug = lang === "en" ? slugs.lettre : "lettre";
---

<Layout
  lang={lang}
  title={t.title}
  description={t.description}
  canonicalPath={`${prefix}/${slug}`}
  themeColor="#FF6B4A"
>
  <section class="screen is-active" data-accent="corail" transition:name="tool-lettre">
    <header class="tool-header">
      <a class="back-btn" href={prefix || "/"} aria-label={common.back}><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 19-7-7 7-7" /><path d="M19 12H5" /></svg></a>
      <h1>{t.h1}</h1>
      <span class="header-spacer"></span>
    </header>
    <div class="tool-body tap-zone" id="lettre-tap-zone">
      <p class="hint">{t.hint}</p>
      <span class="draw-display" id="lettre-display">?</span>
    </div>
    <div class="draw-history" id="lettre-history"></div>
  </section>

  <Fragment slot="scripts">
    <script is:inline src="/js/lettre.js"></script>
    <script is:inline src="/js/app.js"></script>
  </Fragment>
</Layout>
